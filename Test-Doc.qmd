---
title: "test.doc for 9 AM"
author: "Macey Hartmann"
date: "2026-02-20"
format: html
editor: visual
---

# Set up 

```{r}
#| label: packages and data
#| warning: false

library(tidyverse)
library(lterdatasampler)
library(NatParksPalettes)

data(pie_crab) # crab size dataset
```

# Exploratory data visualization 

```{r}
#| label: latitude and size plot
#| warning: false
## base layer: ggplot
ggplot(data = pie_crab, # dataset 
       aes( x = latitude, 
            y = size,
            color = site)) + 
  # first layer: points to represent individual crabs
  geom_point() + 
  scale_color_manual(
    values = natparks.pals("Arches", n = 13, type = "continuous")
      ) +
theme_minimal()
```

